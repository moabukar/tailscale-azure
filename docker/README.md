# Run Tail with

```bash

docker build -t tail

docker run -e TAILSCALE_AUTHKEY=<> tail

## Pulling from DockerHub:

docker pull moabukar/tailscale-azure:a567b7d

docker run -e TAILSCALE_AUTHKEY=<> moabukar/tailscale-azure

```
